{% set comma = joiner(",") %}
{
    "server": {{ (consul_mode == 'server')|lower }},
    "datacenter": "{{ consul_dc_name }}",
    "data_dir": "{{ consul_data_dir }}",
    "log_level": "INFO",
    "enable_syslog": true,
    "start_join": [
    {%- for host in groups['consul-servers-bootstrap'] -%}
    {{ comma() }}"{{ hostvars[inventory_hostname]['ansible_'+consul_network_interface]['ipv4']['address'] }}"
    {%- endfor -%}
    ]
}
